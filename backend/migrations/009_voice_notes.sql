-- ============================================================
-- ONE BHARAT EXPORT-IMPORT — VOICE NOTES IN CHAT
-- ============================================================

ALTER TYPE message_type ADD VALUE IF NOT EXISTS 'voice';
